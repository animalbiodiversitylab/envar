% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protection.R
\name{protection}
\alias{protection}
\title{Download and process WDPA Protected Area layers}
\usage{
protection(x, vars, ...)
}
\arguments{
\item{x}{The output from `par_set()` defining the area or locations for extraction, 
the reference system, and the buffer.
Leave this empty and use `par_set()` to define parameters for download.}

\item{vars}{Character vector of one or more variables to download and process.}

\item{...}{Additional arguments (currently unused).}
}
\value{
If `par_set()` contained a raster/polygon/points with buffer: a `SpatRaster` stack of processed variables.
If `par_set()` contained spatial points or data.frame of points without buffer: a `data.frame` of x, y, and extracted values.
}
\description{
This function downloads, processes, and extracts variables from the
World Database of Protected Areas (WDPA). Each variable corresponds 
to a global raster representing different IUCN Management Categories of 
protected areas.
}
\details{
\strong{Available variables} (working synonyms in parentheses):

\strong{IUCN Management Categories}
\itemize{
  \item "WDPA_IA" ("strict nature reserve", "strict reserve", "1a", "ia", "Ia")
  \item "WDPA_IB" ("wilderness area", "wilderness", "1b", "ib", "Ib")
  \item "WDPA_II" ("national park", "park", "2", "ii", "II")
  \item "WDPA_III" ("natural monument", "monument", "3", "iii", "III")
  \item "WDPA_IV" ("habitat species management", "habitat management", "4", "iv", "IV")
  \item "WDPA_V" ("protected landscape", "protected seascape", "landscape", "5", "v", "V")
  \item "WDPA_VI" ("sustainable use", "natural resources", "6", "vi", "VI")
  \item "WDPA_ALL" ("all", "combined", "full", "total", "all protected areas")
}

\strong{Citation:}\cr
Protected Planet (2025). "World Database of Protected Areas (WDPA)." [UNEP-WCMC].
https://www.protectedplanet.net/en

Note: Users should ensure they comply with the terms of use of the WDPA
when using these data for commercial or research purposes.
}
\examples{
\donttest{
# Example 1: Download National Parks and All Protected Areas for Italy
processed <- par_set(country= "Italy", crs=3035) \%>\% 
protection(vars=c("national park", "WDPA_ALL"))
  }
}

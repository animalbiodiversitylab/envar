1 - The corr_check function now returns warnings for correlation values above 0.6 of pearson and 3 of VIF. Now, in the updated version I want no default warnings and warnings to appear only if the new argument "pearson" and or "vif" is/are not null, so that the user specifies the threshold to get warnings. Store also automatically both correlation plot and a table with the vif in the working directory

2 - in the example code for Parnassius apollo, we should do the following changes: - require(sf) is missing.
- require(ecospat) is missing.
- Replace RMvalues = c(1:8), fc = c("L", "LQ", "LQH") with tune.args = list(fc = c("L", "LQ", "LQH"), rm = 1:8).
- In dismo::predict(), replace predictorsAlps with predictorsAlps$data.
There are some problems with R package problems in this tutorial. For instance, a person said: "This issue appears to be related to a mismatch between the arguments used in ENMevaluate() and the version of the ENMeval package I currently installed. In particular, RMvalues and fc are reported as unused arguments, suggesting incompatibility with the function signature in the installed version.
# Error in ENMeval::ENMevaluate(occs = data[data$pa == "1", c(2:(ncol(data) - :
# unused arguments (RMvalues = c(1:8), fc = c("L", "LQ", "LQH"))" . Is there a way to create a replicable R environemnt with a set of specific R package versions so that it can be exactly replicated by users?

3 - When downloading lots of variables one after another, if connection is suddently lost there might be problems and now the already processed variables might be lost. A person said: "(Optionally) loading datasets from a local copy instead of re-downloading data from sources could provide a substantial performance gain." So it could be worth adding an argument "cache" to the "var_get" function that is TRUE by default but can be set to FALSE. If TRUE, once each variable has been processed to the desired area of interest, resolution etc. it is stored in the cache. If then the R command fails and is interrupted and launched again it starts from where it stopped

4 - I am not sure how envar handles the problem of misalignment of resolution and extent and shifted grids across different sources. How does "envar" handle interpolation/reprojection to the output grid now? Is it the best way possible? How can this behaviour be customized?

5 - add constraints on resolution in case of discrepancies. Is there any internal mechanism to ensure consistency between scale and resolution when a raster is provided? In other words, it should not be possible to use a very fine scale if the raster resolution is 10 km, please clarify whether such constraints are enforced. SO, change the code to make sure that download stops throwing error "The resolution of at least one raster to be downloaded is lower than the set resolution of X km" where X is the parameter res value

6 - we are trying to put it online through GitHub pages but some things are wrong, what should I do? Html or should I do a better readme? Before we used netlify with a private github repository and it worked perfectly